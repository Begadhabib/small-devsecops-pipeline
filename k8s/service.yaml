apiVersion: v1
kind: Service
metadata:
  name: chess-app-service
  namespace: chess
spec:
  type: NodePort
  selector:
    app: chess-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30000
      protocol: TCP
      name: http
